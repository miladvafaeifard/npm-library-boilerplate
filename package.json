{
  "name": "npm-library-boilerplate",
  "version": "0.0.0",
  "description": "{description}",
  "main": "index.js",
  "private": "false",
  "keywords": [],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "tsc",
    "ci/cd": "pnpm run lint && pnpm run test && pnpm run build",
    "publish": "pnpm run lint && pnpm run test && pnpm run build && changeset publish",
    "test": "vitest run"
  },
  "author": "Milad Vafaeifard",
  "license": "MIT",
  "devDependencies": {
    "changesets/cli": "",
    "@types/node": "",
    "typescript": "",
    "tsup": "",
    "vitest": "",
    "zod": ""
  }
}
